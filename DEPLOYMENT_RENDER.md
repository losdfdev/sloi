# Деплой проекта Sloi. на бесплатные сервера Render.com

Render — отличный сервис для размещения веб-приложений. Мы разделим проект на два сервиса: **Web Service** (для Node.js бэкенда) и **Static Site** (для скомпилированного React-фронтенда).

## Шаг 1: Подготовка Github-репозитория
Render автоматически подтягивает код из Github.
1. Убедитесь, что весь ваш код (папки `frontend` и `backend`) загружен в один репозиторий на GitHub.

## Шаг 2: Создание Web Service (Бэкенд)
1. Зайдите на [dashboard.render.com](https://dashboard.render.com/) и нажмите **New +** -> **Web Service**.
2. Выберите ваш GitHub-репозиторий.
3. Настройки для бэкенда:
   - **Name:** слои-backend (или любое)
   - **Root Directory:** `backend` (ВАЖНО! Указываем папку бэкенда)
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `node src/index.js`
   - **Instance Type:** Free (бесплатный тариф)
4. В разделе **Environment Variables** (Advanced) добавьте переменные из вашего `.env`:
   - `PORT` = `5000`
   - `SUPABASE_URL` = `<Ваш URL Supabase>`
   - `SUPABASE_ANON_KEY` = `<Ваш ключ Supabase>`
   - `TELEGRAM_BOT_TOKEN` = `<Ваш токен бота>`
   - `WEBAPP_URL` = *(Оставьте пока пустым, мы вернемся сюда позже, когда развернем фронтенд)*
5. Нажмите **Create Web Service**. 
6. Скопируйте выделенный вам URL (например *https://sloi-backend-xxxx.onrender.com*). Это будет ваш **API URL**.

## Шаг 3: Создание Static Site (Фронтенд)
1. Снова нажмите **New +** -> **Static Site**.
2. Подключите тот же GitHub-репозиторий.
3. Настройки для фронтенда:
   - **Name:** слои-frontend
   - **Root Directory:** `frontend` (ВАЖНО! Указываем папку фронта)
   - **Build Command:** `npm install && npm run build`
   - **Publish directory:** `dist`
4. В разделе **Environment Variables** добавьте:
   - `VITE_API_URL` = `<Ваш URL бэкенда, который вы скопировали на Шаге 2>` (без слеша на конце!)
5. Нажмите **Create Static Site**.
6. Render скомпилирует ваш React и выдаст URL (например *https://sloi-frontend-xxxx.onrender.com*). Это ваш главный адрес приложения (Web App URL).

## Шаг 4: Настройка Telegram Бота
1. Идем в `@BotFather` -> `/mybots` -> выбираем бота Sloi -> **Bot Settings** -> **Menu Button** -> **Configure Menu Button**.
2. Указываем ссылку на фронтенд: `https://sloi-frontend-xxxx.onrender.com`.
3. То же самое делаем для `Inline Keyboard` кнопок внутри кода (настройте переменную `WEBAPP_URL` в панели бэкенда на Render и перезапустите бэкенд).

## Шаг 5: Проверка и финал
- Ваш бот теперь полностью автономен 24/7!
- **Важно:** Бесплатные сервера Render (Free Tier) могут "засыпать", если к ним нет обращений больше 15 минут. Когда пользователь нажмёт `/start` после простоя, включение бэкенда может занять ~30 секунд. Если вам нужна мгновенная загрузка всегда, потребуется план $7/мес.

Поздравляю с релизом! 🎉
